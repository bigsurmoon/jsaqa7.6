{
  "scripts": {
    "cy:open": "cypress run --browser chrome --config-file cypress2.config.js",
    "cy:headed": "cypress run --headed --browser chrome --config-file cypress2.config.js",
    "cy:electron": "cypress run --config-file cypress.config.js",
    "cy:electronHeaded": "cypress run --headed --config-file cypress.config.js"
  },
  "dependencies": {
    "cypress": "^12.1.0"
  }
}
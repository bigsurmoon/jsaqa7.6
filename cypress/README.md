**Запуск тестов с разрешением экрана для ноутбуков:**
```
cypress run --browser chrome --config viewportWidth=1920 viewportHeight=1080
```

**Запуск тестов с разрешением экрана для мобильных телефонов:**
```
cypress run --browser chrome --config viewportWidth=414 viewportHeight=736
```
